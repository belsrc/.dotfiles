# .dotfiles

## Install

```bash
cd
git clone https://github.com/belsrc/.dotfiles.git
cd .dotfiles
bash setup.sh
```

### For dev

To install
`stow -d ~/.dotfiles/stow -t ~ .`

To test
`stow -n -v -d ~/.dotfiles/stow -t ~ .`
